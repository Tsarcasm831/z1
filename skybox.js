import * as THREE from 'three';

export function createSkybox() {
  // Instead of creating geometry that can cause artifacts, return null
  // We'll set the scene background directly in app.js
  return null;
}